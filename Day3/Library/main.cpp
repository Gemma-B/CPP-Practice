#include <iostream>
#include <vector>
std::string book_title_;
int day_;
int month_;
int year_;
bool checked_out_ = false;
int main() {
  
  class library {
    
    class books {
      std::cout << "What book are you checking out?" << std::endl;
      std::cin >> book_title_;
      bool GettingBook ( int& DateCheckedOut(day_, month_, year_), bool checked_out_) {
      if (checked_out_ == false){ 
        DateCheckedOut(day_, month_, year_) 
        std::cout << "cool! you checked out " << book_title << std::endl;
        checked_out_ = true;
      }
      else {
        std::cout << "sorry that book is already checked out!" << std::endl;
      }
      }
    };

    class patrons {
      std::vector<books> BooksOut;
    };
  
  };
  
  return 0;
};
